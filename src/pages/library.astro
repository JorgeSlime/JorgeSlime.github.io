---
import Layout from '../layouts/Layout.astro';
const posts = Object.values(import.meta.glob('./posts/library/*.mdx', { eager: true }));
---

<Layout>
  <section class="container mx-auto p-8">
    <h1 class="text-3xl font-bold text-center">📚 Slime's Library</h1>
    <p class="text-center text-gray-500 mt-4">Explora recursos y estructuras de datos para programación competitiva.</p>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 mt-8">
        {posts.length > 0 ? (
            posts.map((post) => (
                <a href={post.url} class="block p-4 bg-white shadow-md rounded hover:bg-gray-50">
                    <h2 class="text-xl font-semibold">{post.frontmatter.title}</h2>
                    <p class="text-gray-600">{post.frontmatter.description}</p>
                 </a>
            ))
        ) : (
            <p class="text-gray-500">No hay posts disponibles.</p>
        )}

      </div>
    </div>
  </section>
</Layout>

